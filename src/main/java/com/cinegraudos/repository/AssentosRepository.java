package com.cinegraudos.repository;

import com.cinegraudos.domain.entity.AssentosEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AssentosRepository extends JpaRepository<AssentosEntity, Integer> {
}
